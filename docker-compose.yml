server:
  image: "aerospike/aerospike-server:latest"
  ports:
    - "3000:3000"

client:
  build: ./client
  links:
    - server

bench:
  build: ./bench
  links:
    - server

bench-java:
  build: ./bench-java
  links:
    - server